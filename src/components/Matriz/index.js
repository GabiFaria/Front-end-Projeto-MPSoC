import React from 'react';
import './styles.css';

function Matriz () {
    return(
        <div>
            Sketch = (p) => {
                p.setup = () => {

                }

                p.draw = () => {

                }
            }
        </div>
    );
}

export default Matriz;